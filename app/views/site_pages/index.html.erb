<%- model_class = SitePage -%>

		<div class="span12">
		    <div class="thumbnail">
				<% feature_image = @site_pages[2].content %>
				<%= image_tag(feature_image) %>

			  <div class="thumbnail-content">
		      	<p><%= @site_pages[3].content %></p>
			  </div>
		    </div>
		</div>
	</div>
</div>

<div class="row-fuild">
	<div class="page-header" style="text-align: center; margin-top: 35px; padding-bottom: 0px;">
		 <h2 style="margin-bottom: 5px;">Últimos Posts</h1>
	</div>
</div>


	<% counter = 0 %>
	<ul class="thumbnails" style="margin-left: 0px;">
		<div class="row-fluid">
		<% @posts.each do |post| %>
			<% if counter < 3 %>
				<li class="span4">
				    <div class="thumbnail">
						<%= link_to image_tag(post.cover_image_url), url_for(post) %>
      
					  <div class="thumbnail-content">
				      	<h3><%= link_to post.title, post_path(post) %></h3>
				      	<p><%= post.description %></p>
					  </div>
				    </div>
				</li>
				<% counter = counter + 1 %>
			<% end %>
		<% end %>
	</ul>